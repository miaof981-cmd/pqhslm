# 索引创建对照表

> 打印本文档，边创建边打勾 ✅

---

## 📦 集合创建清单（13个）

**公开展示类（5个）** - 权限：**所有用户可读，仅创建者可写**
- [ ] products - 商品表
- [ ] banners - 轮播图
- [ ] notices - 公告
- [ ] categories - 分类
- [ ] service_qrcodes - 客服二维码

**云函数控制类（8个）** - 权限：**仅创建者可读写**
- [ ] users - 用户表（管理员需查看所有用户）
- [ ] orders - 订单表（买家/画师/客服/管理员按角色可见）
- [ ] buyer_shows - 买家秀（作者可编辑自己的秀）
- [ ] artist_applications - 画师申请（申请人和管理员可见）
- [ ] income_ledger - 收入账本（用户查看自己，管理员审核）
- [ ] withdraw_records - 提现记录（用户查看自己，管理员审核）
- [ ] reward_records - 打赏记录（打赏人、画师、管理员可见）
- [ ] system_admin - 管理员表（存储管理员openid）

---

## 🔍 索引创建清单（26个）

### 1. users 集合（2个索引）

- [ ] **索引1：userId（唯一）**
```json
{"keys": {"userId": 1}, "unique": true}
```

- [ ] **索引2：openid（唯一）**
```json
{"keys": {"openid": 1}, "unique": true}
```

---

### 2. orders 集合（10个索引）⭐ 重点

- [ ] **索引1：orderId（唯一）**
```json
{"keys": {"orderId": 1}, "unique": true}
```

- [ ] **索引2：buyerId**
```json
{"keys": {"buyerId": 1}}
```

- [ ] **索引3：artistId**
```json
{"keys": {"artistId": 1}}
```

- [ ] **索引4：serviceId**
```json
{"keys": {"serviceId": 1}}
```

- [ ] **索引5：status**
```json
{"keys": {"status": 1}}
```

- [ ] **索引6：createTime（倒序）**
```json
{"keys": {"createTime": -1}}
```

- [ ] **索引7：deadline**
```json
{"keys": {"deadline": 1}}
```

- [ ] **索引8：status + createTime**
```json
{"keys": {"status": 1, "createTime": -1}}
```

- [ ] **索引9：buyerId + status**
```json
{"keys": {"buyerId": 1, "status": 1}}
```

- [ ] **索引10：artistId + status**
```json
{"keys": {"artistId": 1, "status": 1}}
```

---

### 3. products 集合（4个索引）

- [ ] **索引1：productId（唯一）**
```json
{"keys": {"productId": 1}, "unique": true}
```

- [ ] **索引2：artistId**
```json
{"keys": {"artistId": 1}}
```

- [ ] **索引3：categoryId**
```json
{"keys": {"categoryId": 1}}
```

- [ ] **索引4：status**
```json
{"keys": {"status": 1}}
```

---

### 4. artist_applications 集合（3个索引）

- [ ] **索引1：userId（唯一）**
```json
{"keys": {"userId": 1}, "unique": true}
```

- [ ] **索引2：artistNumber（唯一）**
```json
{"keys": {"artistNumber": 1}, "unique": true}
```

- [ ] **索引3：status**
```json
{"keys": {"status": 1}}
```

---

### 5. income_ledger 集合（4个索引）

- [ ] **索引1：userId**
```json
{"keys": {"userId": 1}}
```

- [ ] **索引2：orderId**
```json
{"keys": {"orderId": 1}}
```

- [ ] **索引3：incomeType**
```json
{"keys": {"incomeType": 1}}
```

- [ ] **索引4：createTime（倒序）**
```json
{"keys": {"createTime": -1}}
```

---

### 6. withdraw_records 集合（3个索引）

- [ ] **索引1：userId**
```json
{"keys": {"userId": 1}}
```

- [ ] **索引2：status**
```json
{"keys": {"status": 1}}
```

- [ ] **索引3：time（倒序）**
```json
{"keys": {"time": -1}}
```

---

## ✅ 最终检查

| 集合名 | 应有索引数 | 实际索引数 | 状态 |
|--------|-----------|-----------|------|
| users | 2 | ___ | ⬜ |
| orders | 10 | ___ | ⬜ |
| products | 4 | ___ | ⬜ |
| artist_applications | 3 | ___ | ⬜ |
| income_ledger | 4 | ___ | ⬜ |
| withdraw_records | 3 | ___ | ⬜ |
| **总计** | **26** | ___ | ⬜ |

---

## 📝 快速操作流程

1. **创建集合**：云开发控制台 → 数据库 → "+" → 输入集合名 → ⚠️ 选择对应权限（见上方清单）→ 确定
2. **创建索引**：点击集合 → 索引管理 → 添加索引 → 切换到JSON模式 → 粘贴JSON → 确定
3. **打勾确认**：每创建一个索引，在对照表上打✅

⚠️ **权限设置特别重要**：
- **公开展示类**（products、banners等5个）→ "所有用户可读，仅创建者可写"
- **云函数控制类**（users、orders等8个）→ "仅创建者可读写"
- **设置错误会导致轮播图、商品列表空白！**

💡 **为什么users/orders也是"仅创建者可读写"？**
- 这些集合涉及多角色权限（买家、画师、客服、管理员）
- 直接设置数据库权限无法满足复杂的访问规则
- 通过云函数API可以灵活控制：
  - 用户只能看自己的信息
  - 画师只能看分配给自己的订单
  - 管理员可以看所有数据
- 详见《云数据库权限-最终修正版.md》

---

**完成后告诉我："集合和索引创建完成"**

