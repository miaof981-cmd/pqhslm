# 索引创建对照表

> 打印本文档，边创建边打勾 ✅

---

## 📦 集合创建清单（12个）

- [ ] users
- [ ] orders
- [ ] products
- [ ] artist_applications
- [ ] categories
- [ ] notices
- [ ] banners
- [ ] income_ledger
- [ ] withdraw_records
- [ ] reward_records
- [ ] service_qrcodes
- [ ] buyer_shows

---

## 🔍 索引创建清单（26个）

### 1. users 集合（2个索引）

- [ ] **索引1：userId（唯一）**
```json
{"keys": {"userId": 1}, "unique": true}
```

- [ ] **索引2：openid（唯一）**
```json
{"keys": {"openid": 1}, "unique": true}
```

---

### 2. orders 集合（10个索引）⭐ 重点

- [ ] **索引1：orderId（唯一）**
```json
{"keys": {"orderId": 1}, "unique": true}
```

- [ ] **索引2：buyerId**
```json
{"keys": {"buyerId": 1}}
```

- [ ] **索引3：artistId**
```json
{"keys": {"artistId": 1}}
```

- [ ] **索引4：serviceId**
```json
{"keys": {"serviceId": 1}}
```

- [ ] **索引5：status**
```json
{"keys": {"status": 1}}
```

- [ ] **索引6：createTime（倒序）**
```json
{"keys": {"createTime": -1}}
```

- [ ] **索引7：deadline**
```json
{"keys": {"deadline": 1}}
```

- [ ] **索引8：status + createTime**
```json
{"keys": {"status": 1, "createTime": -1}}
```

- [ ] **索引9：buyerId + status**
```json
{"keys": {"buyerId": 1, "status": 1}}
```

- [ ] **索引10：artistId + status**
```json
{"keys": {"artistId": 1, "status": 1}}
```

---

### 3. products 集合（4个索引）

- [ ] **索引1：productId（唯一）**
```json
{"keys": {"productId": 1}, "unique": true}
```

- [ ] **索引2：artistId**
```json
{"keys": {"artistId": 1}}
```

- [ ] **索引3：categoryId**
```json
{"keys": {"categoryId": 1}}
```

- [ ] **索引4：status**
```json
{"keys": {"status": 1}}
```

---

### 4. artist_applications 集合（3个索引）

- [ ] **索引1：userId（唯一）**
```json
{"keys": {"userId": 1}, "unique": true}
```

- [ ] **索引2：artistNumber（唯一）**
```json
{"keys": {"artistNumber": 1}, "unique": true}
```

- [ ] **索引3：status**
```json
{"keys": {"status": 1}}
```

---

### 5. income_ledger 集合（4个索引）

- [ ] **索引1：userId**
```json
{"keys": {"userId": 1}}
```

- [ ] **索引2：orderId**
```json
{"keys": {"orderId": 1}}
```

- [ ] **索引3：incomeType**
```json
{"keys": {"incomeType": 1}}
```

- [ ] **索引4：createTime（倒序）**
```json
{"keys": {"createTime": -1}}
```

---

### 6. withdraw_records 集合（3个索引）

- [ ] **索引1：userId**
```json
{"keys": {"userId": 1}}
```

- [ ] **索引2：status**
```json
{"keys": {"status": 1}}
```

- [ ] **索引3：time（倒序）**
```json
{"keys": {"time": -1}}
```

---

## ✅ 最终检查

| 集合名 | 应有索引数 | 实际索引数 | 状态 |
|--------|-----------|-----------|------|
| users | 2 | ___ | ⬜ |
| orders | 10 | ___ | ⬜ |
| products | 4 | ___ | ⬜ |
| artist_applications | 3 | ___ | ⬜ |
| income_ledger | 4 | ___ | ⬜ |
| withdraw_records | 3 | ___ | ⬜ |
| **总计** | **26** | ___ | ⬜ |

---

## 📝 快速操作流程

1. **创建集合**：云开发控制台 → 数据库 → "+" → 输入集合名 → 选择"仅创建者可读写" → 确定
2. **创建索引**：点击集合 → 索引管理 → 添加索引 → 切换到JSON模式 → 粘贴JSON → 确定
3. **打勾确认**：每创建一个索引，在对照表上打✅

---

**完成后告诉我："集合和索引创建完成"**

